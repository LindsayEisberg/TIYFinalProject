
(function () {
  "use strict";

  angular.module('app')
    .controller('MainController', function($scope, $location, $window) {
      var mainCtrl = this;
      // mainCtrl.joinRoom = function() {
      //   $location.path('/auditorium/' + {{room}});
      // }
    });
})();
